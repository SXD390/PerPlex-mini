# Core LangGraph and LangChain dependencies
langgraph>=0.2.0
langchain>=0.3.0
langchain-google-genai>=2.0.0

# AWS and cloud services
boto3>=1.35.0

# Web framework and real-time communication
flask>=3.0.0
flask-socketio>=5.3.0

# Environment and configuration
python-dotenv>=1.0.0

# Text processing and web scraping
beautifulsoup4>=4.12.0
tldextract>=5.1.0

# MCP (Model Context Protocol) server
mcp>=1.0.0

# Data validation and serialization
pydantic>=2.0.0

# Standard library dependencies (included with Python 3.11+)
# - os, json, logging, threading, time, datetime
# - pathlib, uuid, re, base64, random
# - concurrent.futures, asyncio
# - typing (TypedDict, Dict, List, Optional, Any)
# - logging.handlers (RotatingFileHandler)